[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Next.js App Router configuration
# The @netlify/plugin-nextjs handles routing automatically
# No need to specify publish directory - plugin handles it
